## 21:40 记录

### 今日完成
- 完成缺陷管理模块完整实现（后端 + 前端）
  - 数据模型：Defect 模型，包含严重程度、优先级、状态、类型等枚举
  - CRUD 操作：创建、查询、更新、删除缺陷
  - API 路由：8 个接口（列表、详情、创建、更新、删除、状态更新、统计、枚举值、模块列表）
  - 前端页面：项目选择、统计卡片、筛选、列表、详情、新增/编辑对话框
  - 导航菜单：添加"缺陷管理"入口
  - 数据库迁移：成功应用
- 修复缺陷管理页面两个问题：
  - loading 状态未正确重置导致页面一直转圈
  - API 请求未携带 Bearer Token 导致认证失败

### 关键提示/结论
- 前端 API 调用必须使用 `apiGet/apiPost/apiPut/apiDelete` 封装函数，不要用原生 fetch
- 封装函数会自动添加 `Authorization: Bearer {token}` 头
- `loading` 状态在所有分支（成功/失败/无数据）都需要正确重置

### 变更点（文件/模块）
- backend: app/models/defect.py（新增）
- backend: app/crud/defect.py（新增）
- backend: app/api/routes/defects.py（新增）
- backend: app/api/main.py（修改，注册路由）
- backend: app/models/__init__.py（修改，导出模型）
- backend: app/models.py（修改，导出模型）
- backend: app/alembic/versions/40b4b8762289_add_defect_table.py（新增，迁移文件）
- frontend: src/routes/_layout/defects.tsx（新增）
- frontend: src/components/Sidebar/AppSidebar.tsx（修改，添加菜单）
- docs: issues/issues.md（新增两条问题记录）

### 未解决/风险
- 缺陷管理页面未实际测试新增/编辑/删除功能
- 缺陷与测试执行记录的关联功能未实现（execution_id 字段已定义但未使用）

### 明日/下一步
- 测试缺陷管理完整流程
- 考虑实现缺陷与测试执行的关联
- 根据系统迭代日志，下一步可考虑：任务执行重试机制、任务执行超时配置

### 复现/验证方式
- 访问 http://localhost:5173/defects 查看缺陷管理页面
- 选择项目后可查看统计卡片和缺陷列表
- 点击"新增缺陷"可打开创建对话框
- 后端启动：`python -m app.scripts.spawn_dev`（在 backend 目录）
- 前端启动：`npm run dev`（在 frontend 目录）
